作业：

1 完成当前项目中四张表对应的模型对象和对应的DAO（基于DBUtils）。

2 除了每个DAO常用的五个方法，考虑增加借书还书有关的查询：
例如：某用户当前的借书列表
例如：当用户点击可借阅图书时，返回一个可借阅的book_item的id
例如：当用户点击借书列表中的还书按钮时，可以得到book_item的id
注：其实上面两个需求在实现上是一致的。

例如：借书的时候，如何添加当前系统时间和应归还时间。
注：可以利用Calendar类进行时间操作，也可以利用第三方中的工具对时间进行加减操作。

要求：DAO方法要有单元测试。

